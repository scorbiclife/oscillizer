/**
 * A rule is implemented as a function
 * that receives a pattern
 * and returns the pattern iterated to the next generation.
 * @typedef SimpleRule
 * @type {function}
 * @param {SimplePattern} pattern - The pattern to operate on.
 * @returns {SimplePattern} - The pattern at the next generation.
 */
