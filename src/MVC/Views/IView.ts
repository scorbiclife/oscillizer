/**
 * A common interface that defines a view.
 * @typedef {Object} IView
 * @property {function(Event): void} update
 *  A callback to update the view.
 *  The same precautions in {@link IController} apply.
 */
