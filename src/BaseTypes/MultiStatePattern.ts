/**
 * The `MultiStatePattern` datatype that represents multiple cells,
 * with their respective states.
 * This is used for exchaging multi-state pattern data throughout the codebase.
 * For rationale, see {@link TwoStatePattern}.
 *
 * @typedef MultiStatePattern
 * @type {Array<CellEntry>}
 */
